# دليل النشر الشامل (Complete Deployment Guide)

هذا الدليل يشرح بالتفصيل كيفية نشر مدونة "دَوِّنْ" على مختلف منصات الاستضافة.

بفضل استخدام **Astro** و **Keystatic** كـ "Local CMS"، الموقع هو عبارة عن ملفات ثابتة (Static Site) بالكامل، مما يجعله قابلاً للنشر على **أي منصة استضافة**.

---

## 📋 جدول المحتويات

1. [الاستضافة التقليدية (cPanel/Hostinger)](#1-الاستضافة-التقليدية)
2. [Cloudflare Pages](#2-cloudflare-pages)
3. [Vercel](#3-vercel)
4. [GitHub Pages](#4-github-pages)
5. [منصات أخرى](#5-منصات-أخرى)
6. [إدارة المحتوى بعد النشر](#إدارة-المحتوى-بعد-النشر)
7. [إعدادات النطاق (Domain)](#إعدادات-النطاق-domain)

---

## 1. الاستضافة التقليدية

### 🎯 نظرة عامة
الاستضافة التقليدية (مثل cPanel في Hostinger، GoDaddy، Bluehost) مناسبة إذا كان لديك استضافة مدفوعة مسبقاً أو تريد تحكماً كاملاً.

### 📦 المتطلبات
- حساب استضافة مع cPanel
- Node.js مثبت على جهازك المحلي (للبناء)
- FTP client (اختياري - يمكن استخدام File Manager)

### 📝 الخطوات التفصيلية

#### الخطوة 1: بناء المشروع محلياً

1. افتح Terminal/Command Prompt في مجلد المشروع:
   ```bash
   cd path/to/your/project
   ```

2. نفذ أمر البناء:
   ```bash
   npm run build
   ```

3. انتظر حتى يكتمل البناء. سيظهر مجلد جديد اسمه `dist` يحتوي على جميع ملفات الموقع الجاهزة.

#### الخطوة 2: رفع الملفات إلى الاستضافة

**الطريقة الأولى: File Manager (الأسهل)**

1. سجل الدخول إلى لوحة تحكم cPanel
2. اذهب إلى **File Manager** (مدير الملفات)
3. انتقل إلى مجلد `public_html` (أو المجلد المخصص لنطاقك)
4. اضغط **Upload** (رفع)
5. اختر **جميع** الملفات والمجلدات داخل مجلد `dist` (وليس المجلد نفسه)
6. انتظر حتى يكتمل الرفع

**الطريقة الثانية: FTP**

1. استخدم برنامج FTP مثل FileZilla
2. اتصل بحسابك باستخدام:
   - Host: ftp.yourdomain.com
   - Username: اسم المستخدم من cPanel
   - Password: كلمة المرور من cPanel
   - Port: 21
3. انتقل إلى `public_html`
4. اسحب وأفلت محتويات مجلد `dist` (وليس المجلد نفسه)

#### الخطوة 3: التحقق من النشر

1. افتح متصفحك واذهب إلى نطاقك: `https://yourdomain.com`
2. يجب أن يظهر الموقع كاملاً

### ⚠️ ملاحظات مهمة

- **لا ترفع** مجلد `dist` نفسه، بل **محتوياته** فقط
- إذا كنت تستخدم subdomain، ضع الملفات في المجلد المخصص له
- تأكد من وجود ملف `.htaccess` إذا كنت تستخدم Apache

### 🔄 التحديث

عند إجراء تعديلات:
1. نفذ `npm run build` مرة أخرى
2. احذف الملفات القديمة من `public_html` (عدا `.htaccess`)
3. ارفع الملفات الجديدة من `dist`

---

## 2. Cloudflare Pages

### 🎯 نظرة عامة
Cloudflare Pages منصة مجانية سريعة جداً مع CDN عالمي مدمج. مثالية للمواقع الثابتة.

### 📦 المتطلبات
- حساب Cloudflare (مجاني)
- مستودع GitHub يحتوي على مشروعك

### 📝 الخطوات التفصيلية

#### الخطوة 1: رفع المشروع على GitHub

إذا لم تكن قد رفعت المشروع بعد:
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/username/your-repo.git
git push -u origin main
```

#### الخطوة 2: إنشاء مشروع في Cloudflare Pages

1. اذهب إلى [Cloudflare Dashboard](https://dash.cloudflare.com)
2. من القائمة الجانبية، اختر **Pages**
3. اضغط **Create a project**
4. اضغط **Connect to Git**
5. اختر **GitHub** ووافق على الأذونات المطلوبة

#### الخطوة 3: اختيار المستودع

1. اختر المستودع (Repository) الذي يحتوي على مشروعك
2. اضغط **Begin setup**

#### الخطوة 4: إعدادات البناء

أدخل الإعدادات التالية:

- **Project name**: اختر اسماً فريداً (سيصبح `your-project.pages.dev`)
- **Production branch**: `main` (أو `master` حسب مستودعك)
- **Framework preset**: اختر `Astro` (سيملأ الحقول تلقائياً)
- **Build command**: `npm run build`
- **Build output directory**: `dist`

#### الخطوة 5: النشر

1. اضغط **Save and Deploy**
2. انتظر حتى يكتمل البناء (عادة 1-3 دقائق)
3. عند الانتهاء، ستحصل على رابط: `https://your-project.pages.dev`

### 🔄 النشر التلقائي

كل مرة تدفع (push) تحديثات إلى GitHub، سيتم بناء ونشر الموقع تلقائياً!

### 🌐 ربط نطاق مخصص

1. من صفحة المشروع في Cloudflare Pages، اذهب إلى **Custom domains**
2. اضغط **Set up a custom domain**
3. أدخل نطاقك (مثل `blog.yourdomain.com`)
4. اتبع التعليمات لتحديث DNS records

---

## 3. Vercel

### 🎯 نظرة عامة
Vercel منصة سحابية سريعة ومتطورة، مصممة خصيصاً لمشاريع JavaScript/TypeScript.

### 📦 المتطلبات
- حساب Vercel (مجاني)
- مستودع GitHub

### 📝 الخطوات التفصيلية

#### الخطوة 1: رفع المشروع على GitHub
(نفس خطوات Cloudflare أعلاه)

#### الخطوة 2: استيراد المشروع

1. اذهب إلى [Vercel Dashboard](https://vercel.com/dashboard)
2. اضغط **Add New...** ثم **Project**
3. اضغط **Import** بجانب مستودع GitHub الخاص بك
4. إذا لم يظهر المستودع، اضغط **Adjust GitHub App Permissions**

#### الخطوة 3: إعدادات المشروع

Vercel سيكتشف تلقائياً أن المشروع مبني بـ Astro ويضبط الإعدادات:

- **Framework Preset**: Astro
- **Build Command**: `npm run build`
- **Output Directory**: `dist`

يمكنك تركها كما هي أو تعديلها حسب الحاجة.

#### الخطوة 4: النشر

1. اضغط **Deploy**
2. انتظر حتى يكتمل البناء (1-3 دقائق)
3. ستحصل على رابط: `https://your-project.vercel.app`

### 🔄 النشر التلقائي

كل push إلى GitHub = نشر تلقائي!

### 🌐 ربط نطاق مخصص

1. من إعدادات المشروع، اذهب إلى **Domains**
2. اضغط **Add**
3. أدخل نطاقك واتبع التعليمات

---

## 4. GitHub Pages

### 🎯 نظرة عامة
GitHub Pages خيار مجاني مناسب إذا كان مشروعك على GitHub أصلاً.

### 📦 المتطلبات
- حساب GitHub
- المشروع مرفوع على GitHub

### 📝 الخطوات التفصيلية

#### الخطوة 1: إنشاء GitHub Action للبناء والنشر

1. في مجلد المشروع، أنشئ مجلد `.github/workflows`
2. أنشئ ملف `.github/workflows/deploy.yml`:

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main] # أو master حسب اسم الفرع

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

#### الخطوة 2: تفعيل GitHub Pages

1. اذهب إلى صفحة المستودع على GitHub
2. اضغط **Settings**
3. من القائمة الجانبية، اختر **Pages**
4. في **Source**، اختر **GitHub Actions**

#### الخطوة 3: النشر

1. ارفع ملف `deploy.yml` إلى GitHub:
   ```bash
   git add .github/workflows/deploy.yml
   git commit -m "Add GitHub Pages deployment"
   git push
   ```

2. اذهب إلى تبويب **Actions** في المستودع
3. شاهد سير عملية البناء والنشر
4. عند الانتهاء، سيكون موقعك متاحاً على: `https://username.github.io/repository-name/`

### ⚠️ ملاحظة مهمة: Base URL

إذا كان المشروع في subfolder (مثل `username.github.io/blog/`)، عدّل `astro.config.mjs`:

```javascript
export default defineConfig({
  site: 'https://username.github.io',
  base: '/repository-name', // اسم المستودع
  // ... باقي الإعدادات
});
```

---

## 5. منصات أخرى

### Netlify
على الرغم من إزالة إعداداته من المشروع، يمكنك استخدامه:
1. اربط مستودع GitHub
2. Build command: `npm run build`
3. Publish directory: `dist`

### Render
1. أنشئ Static Site جديد
2. اربط GitHub
3. Build command: `npm run build`
4. Publish directory: `dist`

### AWS S3 + CloudFront
للمشاريع الكبيرة:
1. انشر `dist` على S3 bucket
2. فعّل Static Website Hosting
3. استخدم CloudFront للـ CDN

---

## إدارة المحتوى بعد النشر

### ⚠️ ملاحظة هامة

**Keystatic يعمل محلياً فقط** - لوحة التحكم غير متاحة في الموقع المنشور.

### كيف أدير المحتوى؟

#### الطريقة الموصى بها 🥇

1. شغّل الموقع **محلياً** على جهازك:
   ```bash
   npm run dev
   ```

2. افتح لوحة التحكم:
   ```
   http://localhost:4321/keystatic
   ```

3. اكتب مقالاتك وعدّل المحتوى

4. احفظ التغييرات (ستُحفظ في `src/content/`)

5. ارفع التغييرات إلى GitHub:
   ```bash
   git add .
   git commit -m "إضافة مقال جديد"
   git push
   ```

6. **للاستضافات السحابية** (Vercel, Cloudflare, GitHub Pages):
   - سيتم بناء ونشر الموقع **تلقائياً**

7. **للاستضافة التقليدية**:
   - نفذ `npm run build` مرة أخرى
   - ارفع الملفات المحدثة من `dist`

---

## إعدادات النطاق (Domain)

بعد النشر، حدّث رابط الموقع في إعدادات المشروع:

1. افتح `astro.config.mjs`

2. عدّل السطر التالي:
   ```javascript
   export default defineConfig({
     site: 'https://your-actual-domain.com', // ضع رابط موقعك الحقيقي
     // ...
   });
   ```

3. هذا مهم لـ:
   - إنشاء Sitemap صحيح
   - روابط Open Graph للمشاركة على وسائل التواصل
   - Canonical URLs للـ SEO

4. بعد التعديل:
   ```bash
   git add astro.config.mjs
   git commit -m "تحديث رابط الموقع"
   git push
   ```

---

## 🆘 المساعدة والدعم

إذا واجهت أي مشكلة:
- راجع [دليل المطور](DEVELOPER_GUIDE.md) للتفاصيل التقنية
- افتح Issue على GitHub
- تواصل مع المطور: support@3aai.in

---

**نصيحة أخيرة**: ابدأ بـ **Cloudflare Pages** أو **Vercel** للحصول على أفضل تجربة (نشر تلقائي، SSL مجاني، سرعة عالية). استخدم الاستضافة التقليدية فقط إذا كان لديك استضافة مدفوعة مسبقاً.
