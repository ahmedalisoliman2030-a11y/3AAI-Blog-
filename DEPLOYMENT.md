# دليل النشر (Deployment Guide)

هذا الدليل يشرح كيفية نشر مدونة "دَوِّنْ" على الإنترنت.

بفضل استخدام **Keystatic** كـ "Local CMS"، عملية النشر أصبحت أبسط بكثير، حيث أن الموقع هو عبارة عن ملفات ثابتة (Static Site) بالكامل.

---

## 🚀 الخيار الموصى به: الاستضافة التقليدية

الاستضافة التقليدية (مثل cPanel أو Hostinger) هي الخيار الأفضل عندما تريد تحكماً كاملاً في موقعك.

### الخطوات:

1. **بناء المشروع:**
   في مجلد المشروع على جهازك، نفذ الأمر:
   ```bash
   npm run build
   ```

2. **الحصول على الملفات الجاهزة:**
   سيظهر مجلد `dist` يحتوي على جميع ملفات الموقع الثابتة.

3. **رفع الملفات إلى الاستضافة:**
   - سجل الدخول إلى لوحة التحكم (cPanel).
   - اذهب إلى مدير الملفات (File Manager).
   - ارفع محتويات مجلد `dist` إلى مجلد `public_html` (أو المجلد المخصص لموقعك).

🎉 **مبروك!** موقعك الآن منشور ويعمل!

---

## ☁️ خيارات استضافة سحابية أخرى

بما أن الموقع Static، يمكنك نشره على أي منصة استضافة تدعم المواقع الساكنة.

### Vercel
1. استورد المشروع من GitHub إلى Vercel.
2. Vercel سيكتشف أنه مشروع Astro ويضبط الإعدادات تلقائياً.
3. اضغط Deploy.

### Cloudflare Pages
1. اربط مستودع GitHub بـ Cloudflare Pages.
2. ضع إعدادات البناء:
   - **Build command:** `npm run build`
   - **Build output directory:** `dist`
3. انشر الموقع.

---

## ⚠️ ملاحظة هامة بخصوص لوحة التحكم (Keystatic)

في الوضع الافتراضي، **Keystatic يعمل فقط محلياً (`localhost`)**.

عند نشر الموقع، رابط `/keystatic` **لن يعمل** (أو لن يسمح لك بالحفظ) لأن الخادم لا يستطيع الكتابة على ملفات GitHub مباشرة من المتصفح في البيئة الحية (Production) بدون إعدادات إضافية.

### كيف أدير المحتوى بعد النشر؟

لديك خياران:

#### الخيار 1: الطريقة البسيطة (الموصى بها) 🥇
1. شغّل الموقع **محلياً** على جهازك (`npm run dev`).
2. افتح `localhost:4321/keystatic`.
3. اكتب مقالاتك وعدّل المحتوى.
4. احفظ التغييرات.
5. ارفع التغييرات إلى GitHub (`git push`).
6. أعد بناء الموقع ورفعه للاستضافة (أو ستتم العملية تلقائياً إذا كنت تستخدم استضافة سحابية مثل Vercel أو Cloudflare Pages).

**المميزات:**
- لا يتطلب أي إعدادات معقدة.
- آمن جداً.
- مجاني تماماً.

#### الخيار 2: تفعيل Keystatic في الوضع السحابي (GitHub Mode)
إذا كنت تريد الكتابة من المتصفح مباشرة على الموقع الحي (مثلاً من الآيباد أو الهاتف):

1. ستحتاج لإنشاء **GitHub OAuth App**.
2. ربط Keystatic بمشروع GitHub الخاص بك.
3. هذا يتطلب إعدادات إضافية في `keystatic.config.ts`.
   *(راجع [وثائق Keystatic - GitHub Mode](https://keystatic.com/docs/github-mode) للتفاصيل).*

---

## 🔧 إعدادات النطاق (Domain)

بعد النشر، لا تنسَ تحديث رابط الموقع في ملف الإعدادات لضمان عمل الـ SEO و Sitemap بشكل صحيح.

1. افتح `src/config/site.ts`.
2. عدّل قيمة `url`:
   ```typescript
   export const siteConfig = {
     // ...
     url: 'https://www.your-domain.com', // رابط موقعك الجديد
   };
   ```
3. ارفع التغيير لـ GitHub.
