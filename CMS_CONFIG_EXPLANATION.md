# 🔐 شرح إعدادات لوحة التحكم (Decap CMS)

## ❓ ما هو ملف config.yml؟

**الموقع:** `public/admin/config.yml`

هذا الملف يحدد كيف تعمل **لوحة التحكم** (`/admin`) التي تستخدمها لكتابة المقالات.

---

## 🔧 ما تم تحديثه؟

### قبل التحديث ❌

```yaml
backend:
  name: git-gateway  # يتطلب إعداد Netlify Identity
```

**المشكلة:**
- `git-gateway` يحتاج إعداد معقد على Netlify
- لن تستطيع تسجيل الدخول للوحة التحكم
- يتطلب خطوات إضافية (Netlify Identity)

---

### بعد التحديث ✅

```yaml
backend:
  name: github
  repo: ahmedalisoliman2030-a11y/dawen  # مستودعك على GitHub
  branch: main
```

**الفائدة:**
- ✅ تسجيل دخول مباشر بحساب GitHub
- ✅ لا حاجة لإعدادات إضافية
- ✅ أسهل وأسرع

---

## 🎯 كيف يعمل؟

### 1. عندما تذهب إلى `/admin`

```
https://demo-dawen.netlify.app/admin
```

### 2. ستظهر صفحة تسجيل الدخول

- اضغط "Login with GitHub"
- سجّل دخول بحساب GitHub الخاص بك

### 3. Decap CMS سيطلب صلاحيات

سيطلب منك السماح بـ:
- ✅ قراءة المستودع
- ✅ الكتابة على المستودع
- ✅ إنشاء commits

### 4. بعد الموافقة

- ✅ ستدخل للوحة التحكم
- ✅ يمكنك كتابة مقالات جديدة
- ✅ تعديل المقالات الموجودة
- ✅ رفع صور

---

## 📝 ماذا يعني `repo: ahmedalisoliman2030-a11y/dawen`؟

هذا هو **اسم المستودع على GitHub** بالصيغة:

```
اسم_المستخدم/اسم_المستودع
```

**في حالتك:**
- **اسم المستخدم:** `ahmedalisoliman2030-a11y`
- **اسم المستودع:** `dawen` (أو `3AAI-Blog-` إذا لم تغيره بعد)

---

## 🔍 كيف أعرف اسم المستودع الصحيح؟

### الطريقة 1: من رابط GitHub

إذا كان رابط مستودعك:
```
https://github.com/ahmedalisoliman2030-a11y/3AAI-Blog-
```

إذن الـ `repo` يكون:
```yaml
repo: ahmedalisoliman2030-a11y/3AAI-Blog-
```

### الطريقة 2: من Git

```bash
git remote -v
```

سيظهر شيء مثل:
```
origin  https://github.com/ahmedalisoliman2030-a11y/3AAI-Blog-.git
```

خذ الجزء بين `github.com/` و `.git`:
```
ahmedalisoliman2030-a11y/3AAI-Blog-
```

---

## ⚠️ ملاحظة مهمة

### إذا لم تغير اسم المستودع بعد:

المستودع الحالي اسمه: `3AAI-Blog-`

**يجب تحديث config.yml إلى:**
```yaml
repo: ahmedalisoliman2030-a11y/3AAI-Blog-
```

### إذا غيرت اسم المستودع إلى `dawen`:

**يجب تحديث config.yml إلى:**
```yaml
repo: ahmedalisoliman2030-a11y/dawen
```

---

## 🎬 ماذا يحدث عند كتابة مقال؟

### 1. تكتب مقال في `/admin`
- العنوان: "مقال تجريبي"
- المحتوى: "هذا مقال للتجربة"

### 2. تضغط "Publish"

### 3. Decap CMS يقوم بـ:
```bash
# تلقائياً ينشئ ملف جديد
src/content/blog/مقال-تجريبي.md

# يضيفه للـ Git
git add src/content/blog/مقال-تجريبي.md

# ينشئ commit
git commit -m "Create مقال-تجريبي"

# يرفعه لـ GitHub
git push origin main
```

### 4. Netlify يكتشف التغيير
- يبني الموقع تلقائياً
- ينشر المقال الجديد
- يظهر في الموقع خلال 1-2 دقيقة

---

## ✅ التحديث المطلوب الآن

### الملف: `public/admin/config.yml`

**السطر 7:** حدّث اسم المستودع

```yaml
repo: ahmedalisoliman2030-a11y/3AAI-Blog-  # أو dawen إذا غيرت الاسم
```

### بعد التحديث:

```bash
git add public/admin/config.yml
git commit -m "fix: update CMS backend to GitHub"
git push
```

---

## 🧪 اختبار لوحة التحكم

بعد رفع التحديثات:

1. **اذهب إلى:**
   ```
   https://demo-dawen.netlify.app/admin
   ```

2. **اضغط "Login with GitHub"**

3. **وافق على الصلاحيات**

4. **جرّب كتابة مقال تجريبي!**

---

## 🐛 حل المشاكل

### المشكلة 1: "Error loading config"
**الحل:** تأكد من صحة اسم المستودع في `repo:`

### المشكلة 2: "Not Found" عند تسجيل الدخول
**الحل:** تأكد أن المستودع **عام (Public)** على GitHub

### المشكلة 3: "Unauthorized"
**الحل:** تأكد أن حساب GitHub الذي تسجل به له صلاحيات على المستودع

---

## 📚 مصادر إضافية

- [Decap CMS - GitHub Backend](https://decapcms.org/docs/github-backend/)
- [Decap CMS - Configuration](https://decapcms.org/docs/configuration-options/)

---

**تم التحديث:** 2025-12-02  
**المشروع:** دَوٍن (Dawen)  
**المطور:** أحمد علي عبداللطيف
