---
import MainLayout from "../layouts/MainLayout.astro";
import { siteConfig } from "../config/site";

// قراءة بيانات صفحة الاتصال من ملف JSON
import contactData from "../content/contact/data.json";

const socialLinks = [
    { name: "Twitter", url: siteConfig.social.twitter, icon: "twitter" },
    { name: "GitHub", url: siteConfig.social.github, icon: "github" },
    { name: "LinkedIn", url: siteConfig.social.linkedin, icon: "linkedin" },
];

// استخراج البيانات من الملف
const { pageTitle, pageSubtitle, contactInfo, formLabels, formSettings } =
    contactData;
---

<MainLayout title={pageTitle} description={pageSubtitle}>
    <div class="container">
        <div class="contact-header">
            <h1>{pageTitle}</h1>
            <p class="subtitle">
                {pageSubtitle}
            </p>
        </div>

        <div class="contact-grid">
            <!-- Contact Info -->
            <div class="contact-info">
                <div class="info-card">
                    <h3>معلومات التواصل</h3>
                    <p>
                        يمكنك التواصل معنا عبر البريد الإلكتروني أو متابعتنا على
                        وسائل التواصل الاجتماعي.
                    </p>

                    <div class="info-item">
                        <span class="icon">📧</span>
                        <a href={`mailto:${contactInfo.email}`} class="link"
                            >{contactInfo.email}</a
                        >
                    </div>

                    <div class="info-item">
                        <span class="icon">📍</span>
                        <span>{contactInfo.location}</span>
                    </div>

                    {
                        contactInfo.phone && (
                            <div class="info-item">
                                <span class="icon">📞</span>
                                <a
                                    href={`tel:${contactInfo.phone}`}
                                    class="link"
                                >
                                    {contactInfo.phone}
                                </a>
                            </div>
                        )
                    }
                </div>

                <div class="social-links">
                    <h3>تابعنا</h3>
                    <div class="links-wrapper">
                        {
                            socialLinks.map((link) => (
                                <a
                                    href={link.url}
                                    target="_blank"
                                    rel="noopener noreferrer"
                                    class="social-link"
                                >
                                    {link.name}
                                </a>
                            ))
                        }
                    </div>
                </div>
            </div>

            <!-- Contact Form -->
            {
                formSettings.showForm && (
                    <div class="contact-form-wrapper">
                        <form
                            class="contact-form"
                            action={formSettings.formAction || undefined}
                            method={
                                formSettings.formAction ? "POST" : undefined
                            }
                        >
                            <div class="form-group">
                                <label for="name">{formLabels.nameLabel}</label>
                                <input
                                    type="text"
                                    id="name"
                                    name="name"
                                    placeholder={formLabels.namePlaceholder}
                                    required
                                />
                            </div>

                            <div class="form-group">
                                <label for="email">
                                    {formLabels.emailLabel}
                                </label>
                                <input
                                    type="email"
                                    id="email"
                                    name="email"
                                    placeholder={formLabels.emailPlaceholder}
                                    required
                                />
                            </div>

                            <div class="form-group">
                                <label for="subject">
                                    {formLabels.subjectLabel}
                                </label>
                                <input
                                    type="text"
                                    id="subject"
                                    name="subject"
                                    placeholder={formLabels.subjectPlaceholder}
                                    required
                                />
                            </div>

                            <div class="form-group">
                                <label for="message">
                                    {formLabels.messageLabel}
                                </label>
                                <textarea
                                    id="message"
                                    name="message"
                                    rows="5"
                                    placeholder={formLabels.messagePlaceholder}
                                    required
                                />
                            </div>

                            <button type="submit" class="submit-btn">
                                {formLabels.submitButton}
                            </button>
                        </form>
                    </div>
                )
            }
        </div>
    </div>
</MainLayout>

<style>
    .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 2rem;
    }

    .contact-header {
        text-align: center;
        margin-bottom: 3rem;
    }

    h1 {
        font-size: 2.5rem;
        color: var(--color-text);
        margin-bottom: 1rem;
    }

    .subtitle {
        color: var(--color-text-secondary);
        font-size: 1.1rem;
        max-width: 600px;
        margin: 0 auto;
    }

    .contact-grid {
        display: grid;
        grid-template-columns: 1fr 1.5fr;
        gap: 3rem;
    }

    /* Info Section */
    .contact-info {
        display: flex;
        flex-direction: column;
        gap: 2rem;
    }

    .info-card,
    .social-links {
        background: var(--color-surface);
        padding: 2rem;
        border-radius: var(--radius-lg);
        border: 1px solid var(--color-border);
    }

    h3 {
        font-size: 1.25rem;
        margin-bottom: 1rem;
        color: var(--color-text);
    }

    .info-card p {
        color: var(--color-text-secondary);
        margin-bottom: 1.5rem;
        line-height: 1.6;
    }

    .info-item {
        display: flex;
        align-items: center;
        gap: 1rem;
        margin-bottom: 1rem;
        color: var(--color-text);
    }

    .info-item .icon {
        font-size: 1.5rem;
    }

    .info-item .link {
        color: var(--color-text);
        text-decoration: none;
        transition: color 0.2s;
    }

    .info-item .link:hover {
        color: var(--color-primary);
    }

    .links-wrapper {
        display: flex;
        gap: 1rem;
        flex-wrap: wrap;
    }

    .social-link {
        padding: 0.5rem 1rem;
        background: var(--color-surface-hover);
        color: var(--color-text);
        text-decoration: none;
        border-radius: var(--radius-md);
        transition: all 0.2s;
    }

    .social-link:hover {
        background: var(--color-primary);
        color: white;
        transform: translateY(-2px);
    }

    /* Form Section */
    .contact-form-wrapper {
        background: var(--color-surface);
        padding: 2.5rem;
        border-radius: var(--radius-lg);
        border: 1px solid var(--color-border);
    }

    .contact-form {
        display: flex;
        flex-direction: column;
        gap: 1.5rem;
    }

    .form-group {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
    }

    label {
        font-weight: 500;
        color: var(--color-text);
    }

    input,
    textarea {
        padding: 0.75rem;
        border: 1px solid var(--color-border);
        border-radius: var(--radius-md);
        background: var(--color-background);
        color: var(--color-text);
        font-family: inherit;
        transition: border-color 0.2s;
    }

    input:focus,
    textarea:focus {
        outline: none;
        border-color: var(--color-primary);
        box-shadow: 0 0 0 2px var(--color-primary-transparent);
    }

    .submit-btn {
        background: var(--color-primary);
        color: white;
        padding: 0.75rem 1.5rem;
        border: none;
        border-radius: var(--radius-md);
        font-weight: 600;
        cursor: pointer;
        transition:
            background-color 0.2s,
            transform 0.1s;
        align-self: flex-start;
    }

    .submit-btn:hover {
        background: var(--color-primary-dark);
        transform: translateY(-1px);
    }

    .submit-btn:active {
        transform: translateY(0);
    }

    @media (max-width: 768px) {
        .contact-grid {
            grid-template-columns: 1fr;
        }

        .contact-form-wrapper {
            padding: 1.5rem;
        }
    }
</style>
